(()=>{const U='https://ptxdxepmggmjcndgukjk.supabase.co',K='sb_publishable_h3x-jnCW-N8Nx3P6t_D8rA_CS9dgkP-',app=document.getElementById('restaurantAnalyticsApp'),q=new URLSearchParams(location.search),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),session=()=>{try{return JSON.parse(localStorage.getItem('wanba_session')||'null')}catch(e){return null}},api=(p,o={})=>{const s=session();return fetch(U+p,{...o,headers:{apikey:K,Authorization:`Bearer ${s?.access_token||K}`,'Content-Type':'application/json',...(o.headers||{})}})};let days=7,data=null;const close=()=>window.LeshenghuoModuleBridge.back(`/restaurant/manage/?merchant=${encodeURIComponent(q.get('merchant')||'')}`),top=()=>`<header class="top"><button onclick="RestaurantAnalytics.close()">‹</button><b>经营统计</b><button onclick="RestaurantAnalytics.reload()">↻</button></header>`,money=v=>`$${Number(v||0).toFixed(2)}`,metric=(label,value,note)=>`<div class="metric"><span>${label}</span><b>${value}</b><small>${note}</small></div>`;
function render(){const s=data.summary||{},trend=data.trend||[],max=Math.max(1,...trend.map(x=>Number(x.sales||0)));app.innerHTML=`${top()}<section class="hero"><h1>${esc(data.merchant?.business_name||'餐饮店铺')}</h1><p>基于已生成账单、完成订单、会员与优惠券记录统计。支付接入后会自动延续到这里。</p></section><div class="range"><button class="${days===7?'on':''}" onclick="RestaurantAnalytics.range(7)">近 7 天</button><button class="${days===30?'on':''}" onclick="RestaurantAnalytics.range(30)">近 30 天</button></div><section class="metrics">${metric('账单金额',money(s.sales),'已生成账单合计')}${metric('订单数量',Number(s.order_count||0),'已生成账单订单')}${metric('优惠减免',money(s.discount),'账单已记录优惠')}${metric('小费',money(s.tip),'账单已记录小费')}${metric('新会员',Number(s.new_members||0),'本周期加入')}${metric('优惠券核销',Number(s.redeemed_claims||0),'本周期成功核销')}${metric('客单价',money(s.average_order),'账单平均金额')}${metric('待处理订单',Number(s.pending_orders||0),'当前未完成订单')}</section><section class="section"><h2>每日账单趋势</h2><div class="card trend">${trend.length?trend.map(row=>`<div class="trend-row"><span>${esc(row.label)}</span><div class="bar"><i style="width:${Math.max(Number(row.sales||0)?5:0,Math.round(Number(row.sales||0)/max*100))}%"></i></div><b>${money(row.sales)}</b></div>`).join(''):'<div class="empty">这个周期还没有账单记录。</div>'}</div></section><section class="section"><h2>经营概览</h2><div class="card"><div class="line"><b>会员消费记录</b><span>${Number(s.member_transactions||0)} 条</span></div><div class="line"><b>待核销优惠券</b><span>${Number(s.pending_claims||0)} 张</span></div><div class="line"><b>堂食 / 外卖账单</b><span>${Number(s.dinein_bills||0)} / ${Number(s.takeout_bills||0)}</span></div></div></section><p class="note">统计口径：账单金额仅来自已生成的订单账单，未收款或尚未生成账单的订单不会计入销售额。真实线上支付接入后，支付状态将作为独立统计条件。</p>`}
async function load(){if(!q.get('merchant')){app.innerHTML=`${top()}<div class="empty">缺少商家信息。</div>`;return}if(!session()?.access_token){app.innerHTML=`${top()}<div class="empty">请使用店长或具备账单查看权限的员工账号登录。</div>`;return}try{const mr=await api(`/rest/v1/merchants?slug=eq.${encodeURIComponent(q.get('merchant'))}&select=user_id&limit=1`),ms=mr.ok?await mr.json():[],m=ms[0];if(!m?.user_id)throw Error('merchant');const r=await api('/rest/v1/rpc/merchant_restaurant_analytics_snapshot',{method:'POST',body:JSON.stringify({p_merchant_user_id:m.user_id,p_days:days})});if(!r.ok)throw Error(await r.text());data=await r.json();render()}catch(e){app.innerHTML=`${top()}<div class="empty">暂时无法读取经营统计。请确认已运行 v5.421 数据库脚本，并使用拥有账单查看权限的账号登录。</div>`}}window.RestaurantAnalytics={close,reload:load,range:n=>{days=n;load()}};load()})();
