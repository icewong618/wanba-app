# v4.99c 验收清单

- 打开网站，确认版本显示 `v4.99c`。
- 确认根目录存在 `version.json`。
- 浏览器访问 `/version.json?t=任意数字` 可以看到 `4.99c`。
- App 固定入口打开后，不需要重新封装也能显示最新网页。
- 如果 App 仍显示旧页面，完全关闭 App 后重新打开，确认启动页会检查版本。
- GitHub Pages 部署完成后等待 1-3 分钟，再用 App 测试。
- 管理后台、发布页、我的页面、商家主页可以正常进入。
- 首页、详情页、消息页功能没有受到版本检测影响。
