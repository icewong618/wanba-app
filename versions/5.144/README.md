# 乐生活 v5.144

本版新增首页“推荐 / 最新”双排序：推荐会参考关注、点赞、收藏、评论、分类、新鲜度与互动热度；用户可在笔记转发面板选择“减少此类内容”，并在“偏好”中恢复。登录用户运行配套 SQL 后，内容偏好会同步到账号。

## 验收

1. 首页可以切换“推荐”和“最新”。
2. 点开任意笔记的转发面板，可选择“减少此类内容”。
3. 首页“偏好”内可恢复已减少的分类。
4. 运行 `Supabase-feed-preferences-5.144.sql` 后，退出再登录，内容偏好仍会保留。
